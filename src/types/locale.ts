export interface Locale {
  about: {
      "part-one": string;
      "part-two": string;
      "part-three": string;
  };
  experiences: {
    title: string;
    Thoughtworks: {
      role: string;
      period: string;
      resume: string;
      results: string[];
      stack: string[];
    };
    Maxxi: {
      role: string;
      period: string;
      resume: string;
      results: string[];
      stack: string[];
    };
    Lanlink: {
      role: string;
      period: string;
      resume: string;
      stack: string[];
    };
    IMMIFS: {
      role: string;
      period: string;
      resume: string;
      stack: string[];
    };
  };
}